# locale

Translation catalogs for `jksctl` / `jksd`.

Current format:

- `en.po` — default English strings
- `pl.po` — Polish strings

The CLI loads `.po` files directly with a small built-in parser, so no `.mo` compilation step is required.
